# `bin/`

Legacy target directory kept for compatibility with the original tree in
the brief. Actual build outputs go to `build/bin/`.

The CMake rule that produces the CLI places the binary at
`build/bin/minidb` (or `build/bin/Release/minidb.exe` on MSVC). See the
top-level `README.md` for the exact commands.
