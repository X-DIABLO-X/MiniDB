// =============================================================================
// src/transaction/transaction.cpp
// -----------------------------------------------------------------------------
// Transaction is a pure data class defined in the header. This TU exists so
// the module has a stable compilation unit; all behaviour is inline in
// include/transaction/transaction.h.
// =============================================================================
#include "transaction/transaction.h"

namespace minidb::transaction {
    // All Transaction members are defined inline in the header. Nothing
    // to put here.
}
