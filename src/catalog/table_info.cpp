// =============================================================================
// src/catalog/table_info.cpp
// -----------------------------------------------------------------------------
// Translation unit for any non-inline TableInfo helpers. Currently none —
// TableInfo is a POD. This file exists so CMake picks up the folder.
// =============================================================================
//
// TO IMPLEMENT (M0 stub — nothing to do):
//   If we later add a default ctor / hash / equality for TableInfo, put it
//   here. For now keep the file empty so it still compiles.
//
namespace minidb::catalog {
    // intentional no-op
}
